# LocalScholar runs entirely locally and needs NO API key.
# Everything here is optional.

# Point at a different config file.
# LOCALSCHOLAR_CONFIG=./configs/default.yaml

# Store uploads / index somewhere other than ./data
# LOCALSCHOLAR_DATA_DIR=./data
